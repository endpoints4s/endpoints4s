global {
  toc = [
    { title = "Introduction", url = "index.md" }
    use-cases.md
    quick-start.md
    algebras-and-interpreters.md
    design.md
    guides.md
    comparison.md
    talks.md
  ]
}

meta {
  siteTitle = "endpoints "${version}
}

version = ${version}

extension {

  includeCode {
    sourceLinks {
      "../../../../": "https://github.com/julienrf/endpoints/blob/"v${version}"/#ghLines"
    }
  }

  scaladocLinks {
    api {
      index = "unchecked:/api/"${version}"/index.html"
    }
  }
}
